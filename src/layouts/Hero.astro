<!-- HERO-SEKTION -->
<section class="hero">
  <video autoplay muted loop playsinline preload="auto" onloadeddata="this.currentTime=0;">
    <source src="/assets/toki.mp4" type="video/mp4" />
  </video>
</section>

<style>
  /* Desktop hero */
  .hero video {
    position: absolute;
    width: 100%;
    height: 90vh;
    object-fit: cover;
    top: 0;
    left: 0;
    z-index: 0;
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .hero video {
      position: relative;
      width: 87vh;
      height: 52vh;

      object-fit: cover;
      top: 146px;
      left: 0;
      z-index: 0;
    }
  }
</style>
